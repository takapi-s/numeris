.DS_Store
/node_modules/
*.tsbuildinfo

# React Router
/.react-router/
/build/

# Cloudflare
.mf
.wrangler
# 平文の .env / .dev.vars はリポジトリルートの **/.env と **/.dev.vars で無視。
# .env.encrypt はコミット対象のため、.dev.vars* のようなワイルドカードは使わない。

