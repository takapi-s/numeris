# Numeris

このリポジトリは **2つのプロジェクト** を含みます。

- **`modern/`**: app-starter をベースにした現行実装（React Router v7 + SSR/Workers + Supabase）
- **`legacy/`**: 旧CRA実装（Firebase + React Router v6）

## 起動

### modern

`modern/` 配下の README を参照してください。

### legacy

`legacy/` 配下で `npm install` → `npm start`。

